# creare uno script di python che simuli il login
#ad un sito di casinò

#si chieda all'utente: nome cognome ed età
#si verifichi che il nome e il cognome siano più lunghi di un carattere
#e che l'età sia maggiore o uguale di 18 anni

#se tutto è corretto stampare una stringa di conferma
#altrimenti comunicare l'errore all'utente

#si consideri che l'utente fornirà sempre dati del tipo corretto

#BONUS: stampare il resoconto dei dati assicurandosi che nome e cognome inizino con la maiuscola

print(" - - - Casinò Franco - - - \n\n")

name = (input("Nome: "))
surname = (input("Cognome: "))
age = int(input("Età: "))

if (name > str(1)):
    if (surname > str(1)):
        if (age >= (18)):
            print("Benvenuto, dacci tutti i tuoi soldi :)")
            
            name_cap = name.capitalize()
            surname_cap = surname.capitalize()

            print(name_cap, surname_cap, age)


else:
    if (name < str(1)):
        print("Errore nel nome")
if (surname < str(1)):
    print("Errore nel cognome")
if (age < (18)):
    print("Errore nell'età")

